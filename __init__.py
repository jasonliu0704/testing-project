"""Market intelligence pipeline module.

Import concrete APIs from ``app.market_intelligence.pipeline`` or
``app.market_intelligence.image_renderer`` to avoid pulling pipeline
dependencies into renderer-only workflows.
"""
